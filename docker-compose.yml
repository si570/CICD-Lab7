services:
  db:
    image: postgres:17-alpine
    env_file: .env
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

  api:
    build: .
    env_file: .env
    ports:
      - "8000:8080"
    depends_on:
      - db

volumes:
  postgres-data: {}
